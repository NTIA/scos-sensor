# SCOS Sensor Files Directory

This directory is mounted as a Docker volume to `/files` in the scos-sensor Docker
container.

SCOS Sensor stores task result files in this directory on the host (outside the container).
